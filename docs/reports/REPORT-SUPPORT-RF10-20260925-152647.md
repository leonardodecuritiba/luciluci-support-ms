# REPORT — Implementação e prova local RF10

- **status:** `IMPLEMENTED_AND_PROVEN` local, fora de `main`
- **generated_by:** Codex
- **generated_at:** 2026-09-25T15:26:47Z
- **review_mode:** final
- **microservice:** support-ms
- **repository_ref:** `feat/support-rf10-create-message`, base documental `b72c585f0b83679d02bcedf573e38558c17eb4d4`
- **documentation_ref:** Support 0.12 `85c7e958adb0cbb9fa43842de7f990260f2bc0ee`
- **report_file:** `docs/reports/REPORT-SUPPORT-RF10-20260925-152647.md`
- **reviewer:** não identificado

# 1. Resumo executivo

RF10 (`POST /api/support/tickets/{ticketId}/messages`) foi implementada e provada localmente. RF01–RF09 estão integradas em `main`; RF10 está somente nesta branch funcional; RF11–RF13 permanecem indisponíveis. O contrato Support 0.12 e o PRD original não foram alterados. O checkpoint RF10 bloqueado anterior é histórico. Não houve merge, push, PR, CI remoto ou deploy nesta execução.

O risco mais sensível da RF10 é a cardinalidade literal: mensagem de backoffice/cd cria duas auditorias mesmo quando `adminStatus` já é `pendente`. O teste HTTP/SQLite e a prova PostgreSQL física confirmaram esse caso.

# 2. Escopo e fontes analisadas

Autoridade: `luciluci-docs/support/{README,prd,notes,tdd,tp,dependencies}.md` em Support 0.12 e `sources/prd-original.md`. Base funcional: `main == origin/main == 393af3ed50c35fb541825c1822cecaa3b8005a29`; feature criada do checkpoint documental publicado `b72c585`. Código de referência: `src/features/ticket/**`, `src/features/department/**`, `src/shared/openapi/swagger.ts`, migration RF05, `api.http`, testes e provas RF01–RF09. O gitlink permaneceu em `85c7e95`.

CI remoto desta branch: ausente, pois não houve publicação/PR. A CI da PR #9 é evidência histórica da baseline RF09, não da RF10. A prova PostgreSQL usou PostgreSQL 16 em contêiner exclusivo, criou cada banco com nome `support_s1_proof_rfNN_*`, verificou ausência prévia e o descartou no próprio script. O smoke criou e removeu contêineres, rede e tag de imagem próprios.

# 3. Matriz principal RF x implementação

| RF          | Endpoint/área                   | Estado                            | Evidência                                             |
| ----------- | ------------------------------- | --------------------------------- | ----------------------------------------------------- |
| RF01–RF04   | Department                      | Integradas em `main`              | Suítes e provas PostgreSQL RF01–RF04 repetidas        |
| RF05        | Criar Ticket                    | Integrada em `main`               | Suíte e prova PostgreSQL RF05 repetidas               |
| RF06        | Editar Ticket                   | Integrada em `main`               | Suíte, prova PostgreSQL RF06 e concorrência RF10×RF06 |
| RF07a/RF07b | Listar Tickets                  | Integradas em `main`              | Suítes e prova PostgreSQL RF07 repetidas              |
| RF08        | Resolver Ticket                 | Integrada em `main`               | Suíte, prova PostgreSQL RF08 e concorrência RF10×RF08 |
| RF09        | Buscar Ticket por ID            | Integrada em `main`               | Suíte e prova PostgreSQL RF09 repetidas               |
| RF10        | Criar TicketMessage             | Implementada/provada nesta branch | Unitário, HTTP/SQLite, contrato, PostgreSQL, smoke    |
| RF11–RF13   | Visibilidade/listagem/histórico | Não implementadas                 | OpenAPI e prova RF10 confirmam ausência               |

# 4. Checklist consolidado por PRD

- [x] Preservados `type` e `authorId` no body com igualdade obrigatória aos headers.
- [x] Admin por membership atual no Department; solicitante por `requesterId`, sem comparar `Ticket.origin` com o role nem bloquear Department para owner.
- [x] Admin escolhe visibilidade; solicitante envia `true`.
- [x] Mensagem e mídia posicional criadas; admin gera uma `nova_mensagem`; backoffice/cd geram `nova_mensagem` e `alteracao_status/admin/pendente`, mesmo se status já pendente.
- [x] Cada POST válido gera recurso novo, atualiza `Ticket.updatedAt` e retorna `201` com os oito campos da TicketMessage.
- [ ] RF11–RF13 continuam para checkpoints próprios.

# 5. Checklist consolidado por TDD

- [x] DTO estrito, controller RF10, use case sem Express, interface focada e repositório TypeORM reutilizado.
- [x] Transação única; Ticket `FOR UPDATE` primeiro, Department atual `FOR UPDATE` somente para admin; ACL revalidada sob lock.
- [x] Updates estreitos: admin somente `updatedAt`; solicitante somente `adminStatus` e `updatedAt`. RF06 mantém `updateTicket()` próprio; RF08 mantém atualização exclusiva de `requesterStatus`.
- [x] Mesmo instante lógico para Message, Ticket e AuditLogs; UUID v4 independente por registro. Sem `X-Idempotency-Key`, eventos, outbox ou migration nova.
- [x] Prova física de rollback e de concorrência PostgreSQL. A migration RF05 existente suporta `TicketMessage.type=admin`; só a tipagem TypeScript da entidade foi ampliada.

# 6. Checklist consolidado por TP

| RF        | Unitário                                 | Integração                                     | Funcional/contrato                                           | Resultado      |
| --------- | ---------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------ | -------------- |
| RF01–RF09 | Suíte completa executada                 | Suíte completa executada                       | Provas PostgreSQL RF01–RF09 repetidas                        | PASS           |
| RF10      | `create-ticket-message.use-case.spec.ts` | `create-ticket-message.spec.ts` em HTTP/SQLite | `prove-rf10-postgres.js`, contrato OpenAPI e smoke de imagem | PASS           |
| RF11–RF13 | Não aplicável nesta wave                 | Não executado                                  | Rotas permanecem 404                                         | Fora do escopo |

A suíte completa passou com **29 suítes e 262 testes**. Cobertura: 98,03% statements (998/1018), 87,38% branches (284/325), 98,72% functions (155/157) e 98,44% lines (950/965). `coverage:check` é registrado nos gates finais abaixo.

# 7. Inventário de endpoints reais

Novo endpoint: `POST /api/support/tickets/{ticketId}/messages`. Headers obrigatórios: correlação e ator; body estrito com `message,type,authorId,isVisibleToRequester` obrigatórios e `mediaIds` opcional. `201` retorna somente TicketMessage; `400/403/404/422/500` seguem o envelope existente. `api.http`, `src/shared/openapi/swagger.ts` e o artefato `docs/openapi/v1/support-api.json` foram alinhados; compatibilidade contra `origin/main` passou. RF11–RF13 permanecem ausentes do OpenAPI e em 404. Superfícies operacionais `/health`, `/metrics` e `/api-docs*` não são RFs.

# 8. Fronteira NFR e capacidades transversais

| Capacidade                                                | Classificação                | Evidência/limite                                                       |
| --------------------------------------------------------- | ---------------------------- | ---------------------------------------------------------------------- |
| Correlação, erros, autorização fina, transação PostgreSQL | Implementado localmente      | HTTP/SQLite e PostgreSQL/processo compilado RF10                       |
| AuthN e RBAC amplo                                        | Upstream/plataforma          | Contexto de ator vem do BFF; Support compara headers/body e aplica ACL |
| Observabilidade distribuída                               | Compartilhado                | Nenhuma capacidade RF10 adicional especificada                         |
| Eventos, outbox, DLQ, Schema Registry                     | Fora do escopo desta release | Nenhum evento Support aprovado                                         |
| Seed W1                                                   | Gap real local anterior      | Não alterado por RF10; massa determinística ainda depende de decisão   |

# 9. Cobertura de testes, rollback, concorrência e validação

| Rollback PostgreSQL   | HTTP | Message/Media        | Ticket     | Audits      | Cleanup          |
| --------------------- | ---: | -------------------- | ---------- | ----------- | ---------------- |
| R1 media insert       |  500 | Nenhum novo registro | Inalterado | Nenhum novo | Trigger removido |
| R2 Ticket update      |  500 | Nenhum novo registro | Inalterado | Nenhum novo | Trigger removido |
| R3 primeira auditoria |  500 | Nenhum novo registro | Inalterado | Nenhum novo | Trigger removido |
| R4 segunda auditoria  |  500 | Nenhum novo registro | Inalterado | Nenhum novo | Trigger removido |

| Concorrência PostgreSQL  | HTTP    | Mensagens novas | Mídias novas | Auditorias novas | Estado final observado                  | Lost update/deadlock |
| ------------------------ | ------- | --------------: | -----------: | ---------------: | --------------------------------------- | -------------------- |
| RF10 requester×requester | 201/201 |               2 |            2 |                4 | admin pendente; requester não resolvido | Não                  |
| RF10 admin×admin         | 201/201 |               2 |            0 |                2 | admin pendente preservado               | Não                  |
| RF10×RF06                | 201/200 |               1 |            0 |                3 | admin finalizado na ordem observada     | Não                  |
| RF10×RF08                | 201/200 |               1 |            0 |                3 | admin pendente; requester resolvido     | Não                  |

No RF10 requester×requester, cada mídia ficou associada à sua mensagem (`cd:left`, `backoffice:right`); as quatro auditorias foram duas `nova_mensagem` e duas `alteracao_status/admin/pendente`. Nos dois cenários RF10×RF10, `Ticket.updatedAt` coincidiu com o maior `Message.createdAt` da rodada, ou seja, a última operação serial observada.

Gates executados com exit 0: `lint`, `build`, `build:check`, `openapi:export`, `openapi:check`, `openapi:compat` contra `origin/main`, `messaging:check`, `test:unit`, `test:integration`, `test:contract`, `test:coverage`, `coverage:check`, `format:check`, provas PostgreSQL RF01–RF10, `proof:s1:image` e `git diff --check`.

A primeira execução de `test:coverage` dentro do sandbox falhou com `listen EPERM` no bind local do Supertest. A repetição com bind local permitido passou integralmente. A primeira prova RF10 confirmou todos os cenários, mas falhou no cleanup de processo já encerrado (`kill ESRCH`); o cleanup foi corrigido. O reforço posterior da prova de mídia falhou inicialmente por consultar `media.id`, coluna inexistente na tabela com chave composta; a consulta foi corrigida e a prova final passou com exit 0 e banco descartado. A primeira RF02 histórica falhou na interpretação do `timestamp` sob fuso local; o replay com `TZ=UTC` passou, assim como RF03–RF09. Nenhuma dessas falhas foi atribuída a comportamento de RF10.

# 10. Divergências

## 10.1 Documentação prevê, código não comprova

RF11–RF13 continuam planejadas. Nenhum comportamento delas foi implementado por RF10.

## 10.2 Código existe, documentação não comprova

Nenhuma lacuna RF10 encontrada após alinhar OpenAPI, `api.http`, estado local e report. Support 0.12 foi mantida fixa.

## 10.3 `ACTUAL_STATE.md` afirma, código não comprova

O bloco novo distingue prova local de integração em `main`. As fotografias antigas dos checkpoints permanecem históricas.

## 10.4 PRD / TDD / TP divergem entre si

Nenhuma contradição material RF10 nova foi identificada. A cardinalidade de duas auditorias com status já pendente foi preservada literalmente.

## 10.5 Ambiguidades que impedem conclusão segura

Nenhuma no recorte RF10. CI remota e revisão de PR dependem de futura publicação da branch.

# 11. Conclusão

`RF10_IMPLEMENTED_AND_PROVEN` localmente; pronta para revisão de diff e futura PR após publicação explícita. Não foi feito commit, push, PR, merge ou deploy nesta execução. Próximo passo: publicar `feat/support-rf10-create-message` e abrir PR em lote posterior; aguardar integração e checkpoint próprio antes de RF11.
