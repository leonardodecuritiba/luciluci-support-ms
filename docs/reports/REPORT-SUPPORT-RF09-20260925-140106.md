# REPORT — Implementação e prova local RF09 do support-ms

- **status:** `RF09_IMPLEMENTED_AND_PROVEN_LOCAL / NOT_IN_MAIN`
- **generated_by:** Codex
- **generated_at:** 2026-09-25T14:01:06Z
- **review_mode:** final
- **microservice:** support-ms
- **repository_ref:** `feat/support-rf09-get-ticket`, criada de `origin/codex/support-rf09-contract-checkpoint` em `f76c217ea6010ceb9d766b6711e36609e8303945`; `main=origin/main=45be90318bdb71e67532482364933cb49e6660e9`
- **documentation_ref:** `luciluci-docs/support/{README,prd,notes,tdd,tp,dependencies}.md`, Support 0.11 `a198b46c62d4b5cd1a4aa0ced8eb171b2e6ef3b2`
- **report_file:** `docs/reports/REPORT-SUPPORT-RF09-20260925-140106.md`
- **reviewer:** não informado

---

# 1. Resumo executivo

- Há 14 operações RF01–RF13 no PRD, contando RF07a/RF07b separadamente: dez implementadas nesta branch, quatro ainda ausentes (RF10–RF13). RF01–RF08 estão integradas em `main`; RF09 foi implementada e provada localmente nesta branch.
- RF09 expõe `GET /api/support/tickets/{ticketId}` com headers e UUID v4 obrigatórios. Admin usa membership atual do Department; backoffice/cd usam ownership do Ticket. Department inativo continua legível. Resposta tem somente onze campos de Ticket.
- PostgreSQL 16 real com processo compilado comprovou ACL, erros, projeção exata e ausência de escrita em snapshots físicos de seis tabelas. O smoke da imagem com owner e admin passou. Bancos e containers foram descartados.
- Contrato canônico Support 0.11 e gitlink permaneceram intactos. Não há migration, evento, outbox, mensageria, commit, push, PR, merge ou deploy RF09 neste lote.

# 2. Escopo e fontes analisadas

- Autoridade funcional: `luciluci-docs/support/{prd,notes,tdd,tp}.md` Support 0.11; PRD original em `luciluci-docs/support/sources/prd-original.md`. O checkpoint bloqueado e o report de fechamento RF09 são históricos.
- Runtime e contrato: `src/app.ts`, `src/features/ticket/adapters/{routes,controllers,repositories}/`, `src/features/ticket/use-cases/`, `src/shared/openapi/swagger.ts`, `docs/openapi/v1/support-api.json`, `api.http`.
- Testes e operação: `tests/unit/ticket/get-ticket.use-case.spec.ts`, `tests/integration/ticket/get-ticket.spec.ts`, `tests/contract/openapi/openapi.contract.test.ts`, `scripts/prove-rf09-postgres.js`, `scripts/prove-s1-image.js`, `.github/workflows/ci.yml`.
- Método: `.codex/skills/report-review.md`, `docs/reports/REPORT-TEMPLATE.md` e `docs/prompts/report-completeness-prompt.md`. Comparação de baseline pelo `git diff origin/main...origin/codex/support-rf09-contract-checkpoint` e gitlink fixo.

# 3. Matriz principal RF x implementação

| RF          | Contrato                      | Estado nesta branch | Código/contrato/teste/evidência                                                                 |
| ----------- | ----------------------------- | ------------------- | ----------------------------------------------------------------------------------------------- |
| RF01–RF06   | Revisões canônicas anteriores | Implementado        | Runtime anterior; provas PostgreSQL RF01–RF06 repetidas                                         |
| RF07a/RF07b | Support 0.9                   | Implementado        | Runtime anterior; prova PostgreSQL RF07 repetida                                                |
| RF08        | Support 0.10                  | Implementado        | Runtime anterior; prova PostgreSQL RF08 repetida                                                |
| RF09        | Support 0.11                  | Implementado        | Route/controller/use-case/repository, OpenAPI, unit, integration, contract, PostgreSQL e imagem |
| RF10–RF13   | Decisões próprias pendentes   | Não encontrado      | Ausência em OpenAPI e 404 HTTP; sem runtime                                                     |

Não há RF parcial ou ambígua neste recorte. `/health`, `/metrics` e documentação HTTP são superfícies operacionais, não RFs.

# 4. Checklist consolidado por PRD

- [x] GET por ID carrega Ticket antes da ACL; Ticket ausente retorna `404`, Ticket existente sem acesso retorna `403`.
- [x] Admin precisa de membership atual em `allowedUserIds`; `Department.type` não concede acesso e `active=false` não bloqueia Ticket histórico.
- [x] Backoffice/cd precisam ser o requester owner; role não precisa coincidir com `Ticket.origin`.
- [x] Sucesso retorna exatamente `id,number,subject,requesterId,departmentId,priority,origin,adminStatus,requesterStatus,createdAt,updatedAt`.
- [x] Leitura não altera Ticket/updatedAt, Department, membership, mensagem, mídia ou auditoria; não gera evento.
- [x] Erros `400/403/404/422/500`; sem `401/409` local. Qualquer query ou body presente recebe `422`.

# 5. Checklist consolidado por TDD

- [x] Ordem das rotas preserva requester/admin antes do parâmetro de ID e reserva `/history` como 404 RF13.
- [x] Caso de uso `GetTicketUseCase` usa repositório de leitura sem lock, transação de escrita ou mutação. Consulta membership atual apenas para admin, após encontrar o Ticket.
- [x] Controller valida headers, UUID v4, ausência de query e body; middleware recusa body presente antes do parser JSON, inclusive `{}` e `null`.
- [x] OpenAPI GET não possui query/requestBody; define três headers, path UUID v4, Ticket completo e códigos `200/400/403/404/422/500`. O schema novo `TicketDetail` aceita os dois status de requester sem modificar o contrato publicado dos responses RF06/RF08.
- [x] `api.http`, artefato OpenAPI, CI e README/runbook foram sincronizados. Não houve migration, idempotência, AuditLog novo ou mensageria.

# 6. Checklist consolidado por TP

| RF          | Unit                          | Integration          | Functional/contract                                   | Estado            |
| ----------- | ----------------------------- | -------------------- | ----------------------------------------------------- | ----------------- |
| RF01–RF06   | Suítes históricas             | Suítes históricas    | Provas PostgreSQL RF01–RF06 repetidas                 | Passou localmente |
| RF07a/RF07b | Suíte histórica               | Suíte histórica      | Prova PostgreSQL RF07 repetida                        | Passou localmente |
| RF08        | Suíte histórica               | Suíte histórica      | Prova PostgreSQL RF08 repetida                        | Passou localmente |
| RF09        | `get-ticket.use-case.spec.ts` | `get-ticket.spec.ts` | OpenAPI contract, `proof:rf09:postgres`, smoke imagem | Passou localmente |
| RF10–RF13   | Planejado                     | Planejado            | 404/ausência OpenAPI verificados                      | Não implementado  |

O TP canônico mantém fluxos multi-RF FU-SUP-01/02/03/06 como planejamento amplo; esta prova cobre somente o recorte RF09 e pontos de integração RF01/RF05/RF08 pertinentes. Nenhum UAT ou CI remota RF09 foi observado.

# 7. Inventário de endpoints reais

RF09 adiciona somente `GET /api/support/tickets/:ticketId`, sobre o path OpenAPI que já continha `PATCH` RF06. As rotas de listagem RF07 têm precedência; `/api/support/tickets/history` permanece 404. `POST /messages`, `PATCH /messages/{messageId}/visibility` e `GET /messages` seguem 404. O GET exige correlação, ator e role; não aceita query/body.

# 8. Fronteira NFR e capacidades transversais

| Item                                              | Categoria                    | Evidência / limite                                          |
| ------------------------------------------------- | ---------------------------- | ----------------------------------------------------------- |
| Correlação, ACL fina, PostgreSQL, erros e OpenAPI | Implementado localmente      | Testes HTTP, contrato, processo compilado e imagem          |
| AuthN e RBAC amplo                                | Upstream/plataforma          | BFF fornece identidade; Support aplica ACL RF09             |
| Observabilidade distribuída                       | Compartilhado                | Nenhuma obrigação local RF09 adicional especificada         |
| Eventos, outbox, DLQ/redrive, Schema Registry     | Fora do escopo desta release | Nenhum evento Support aprovado                              |
| Seed W1                                           | Gap local prévio             | Massa determinística ainda não aprovada; não deriva de RF09 |

# 9. Cobertura de testes e validação

O primeiro `test:coverage` após a implementação expôs um teste histórico RF05 que ainda esperava RF09=404; ele foi alinhado para verificar somente rotas futuras. A primeira comparação OpenAPI rejeitou a ampliação da enum no schema compartilhado `Ticket`; o GET RF09 passou a usar `TicketDetail`, preservando os responses publicados RF06/RF08. Replays completos posteriores passaram. As suítes somaram 27/27 e 239/239 testes após incluir teste de erro inesperado; cobertura final: statements 97,93%, branches 87,16%, functions 98,62%, lines 98,28%, acima dos limites do gate. Unit, integration e contract foram executados também por seus scripts específicos.

| Gate/prova                                                           | Resultado                                                                                    |
| -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `npm run lint`, `build`, `build:check`                               | Passou                                                                                       |
| `openapi:export`, `openapi:check`, compatibilidade com `origin/main` | Passou                                                                                       |
| `messaging:check`, `format:check`, `git diff --check`                | Passou                                                                                       |
| Unit/integration/contract, `test:coverage`, `coverage:check`         | Passou                                                                                       |
| `proof:rf09:postgres`                                                | Passou no build final; banco `support_s1_proof_rf09_final_1790345033237` criado e descartado |
| `proof:rf01`–`proof:rf08:postgres`                                   | Passou; bancos exclusivos criados/descartados; RF02–RF08 com `TZ=UTC`                        |
| `proof:s1:image`                                                     | Passou; rede, containers e tag exclusiva removidos                                           |

Uma tentativa inicial de replay RF02 com fuso local falhou por leitura de `timestamp` sem fuso pelo cliente histórico; o banco foi descartado e o replay com `TZ=UTC` passou. Isso não altera o contrato nem o runtime RF09. Testes de integração em SQLite não substituem a prova PostgreSQL física. Nenhum run remoto RF09 existe.

# 10. Divergências

## 10.1 Documentação prevê, código não comprova

RF10–RF13 seguem pendentes por contrato próprio. Seed W1 é lacuna anterior, independente da leitura RF09. Os fluxos multi-RF completos do TP aguardam suas RFs.

## 10.2 Código existe, documentação não comprova

Nenhuma divergência identificada no recorte RF09. O GET usa somente campos, status, ACL e erros aprovados na revisão 0.11.

## 10.3 ACTUAL_STATE afirma, código não comprova

Nenhuma afirmação de implementação RF09 depende apenas deste report; paths de código, contrato e provas acima a sustentam.

## 10.4 PRD / TDD / TP divergem entre si

Nenhuma divergência RF09 identificada após o complemento Support 0.11; o PRD original permanece preservado como fonte histórica.

## 10.5 Ambiguidades que impedem conclusão segura

Sem bloqueio para RF09 local. Integração em `main` e CI remota dependem de publicação/PR posterior; não são atribuídas a esta prova.

# 11. Conclusão

RF09 está `IMPLEMENTED_AND_PROVEN_LOCAL` na branch funcional e alinhada ao contrato Support 0.11. `main` continua em `MAIN_BASELINE_RF08`. A implementação está pronta para revisão/publicação em PR, sem avançar RF10 ou acionar deploy. O risco residual é de validação remota/integração ainda não executada, além das RF10–RF13 e seed W1 fora deste recorte.
