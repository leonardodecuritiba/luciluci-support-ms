# REPORT — Implementação e prova local RF08 do support-ms

- **status:** `RF08_IMPLEMENTED_AND_PROVEN_LOCAL / NOT_IN_MAIN`
- **generated_by:** Codex
- **generated_at:** 2026-09-24T21:39:33Z
- **review_mode:** final
- **microservice:** support-ms
- **repository_ref:** `feat/support-rf08-resolve-ticket`, criada de `origin/docs/support-rf08-resolve-ticket-contract` em `513750f95486e15635eed064023a73f52be66683`; `main=origin/main=43a556ab1c70f5de9a63e3e6ab651445fa462173`
- **documentation_ref:** `luciluci-docs/support/{README,prd,notes,tdd,tp}.md`, Support 0.10 `93edf66d6ed0002a2af537339da315db1285a779`
- **report_file:** `docs/reports/REPORT-SUPPORT-RF08-20260924-213933.md`
- **reviewer:** não informado

---

# 1. Resumo executivo

- RF01–RF07b continuam integradas em `MAIN_BASELINE_RF07`. RF08 foi implementada e provada localmente nesta branch; RF09–RF13 continuam sem runtime. Há 14 operações no PRD, nove implementadas na branch, cinco futuras.
- RF08 materializa `POST /api/support/tickets/{ticketId}/resolve` sem body, somente para requester owner `backoffice|cd`. Transição efetiva muda requesterStatus/updatedAt e cria um AuditLog requester em transação; repetição autorizada retorna Ticket completo sem write.
- PostgreSQL 16 real provou rollback de AuditLog, no-op e concorrência RF08×RF08/RF06×RF08 sob lock de Ticket. A imagem de produção passou no smoke com criação e resolução de Ticket.
- O gitlink permanece em Support 0.10 `93edf66d6ed0002a2af537339da315db1285a779`; não houve alteração do repositório canônico, migration, evento, outbox, merge ou deploy funcional neste lote.

# 2. Escopo e fontes analisadas

- Autoridade de contrato: `luciluci-docs/support/{prd,notes,tdd,tp}.md` Support 0.10 e fonte original preservada em `luciluci-docs/support/sources/prd-original.md`. Histórico: report do checkpoint RF08 bloqueado e report contratual 0.10.
- Runtime: `src/features/ticket/adapters/{routes,controllers,repositories}/**`, `src/features/ticket/use-cases/**`, `src/app.ts`, `src/shared/openapi/swagger.ts` e `docs/openapi/v1/support-api.json`.
- Testes: `tests/unit/ticket/resolve-ticket.use-case.spec.ts`, `tests/integration/ticket/resolve-ticket.spec.ts`, `tests/contract/openapi/openapi.contract.test.ts`, provas PostgreSQL RF01–RF08, smoke da imagem e `.github/workflows/ci.yml`.
- Método de report: `.codex/skills/report-review.md`, `docs/reports/REPORT-TEMPLATE.md` e `docs/prompts/report-completeness-prompt.md`. S1 e infraestrutura operacional não foram reimplementados.

# 3. Matriz principal RF x implementação

| RF          | Contrato                         | Runtime na branch | Evidência                                                                |
| ----------- | -------------------------------- | ----------------- | ------------------------------------------------------------------------ |
| RF01–RF06   | Congelado em revisões anteriores | Implementado      | Provas PostgreSQL RF01–RF06 repetidas e passaram                         |
| RF07a/RF07b | Support 0.9                      | Implementado      | Prova PostgreSQL RF07 repetida e passou                                  |
| RF08        | Support 0.10                     | Implementado      | Unit, integration, contract, prova PostgreSQL e smoke da imagem passaram |
| RF09–RF13   | Pendente                         | Não encontrado    | OpenAPI e HTTP mantêm rotas ausentes/404                                 |

Nenhuma RF parcial ou ambígua foi identificada neste recorte. Superfícies `/health`, `/metrics` e `/api-docs` não contam como RFs.

# 4. Checklist consolidado por PRD

- [x] Requester owner executa RF08 mesmo com `ticket.origin` diferente; admin e não dono recebem 403. Department inativo não bloqueia.
- [x] Transição `nao_resolvido -> resolvido` muda somente requesterStatus e updatedAt; auditoria `alteracao_status`, `statusType=requester`, `newStatus=resolvido`, `authorId` e `origin` do ator.
- [x] Ticket resolvido retorna 200/no-op com Ticket completo, sem update ou auditoria novos. Nenhuma mensagem, mídia ou Department expandido no response.
- [x] Qualquer body presente, inclusive `{}`, `null`, texto e JSON malformado, recebe 422 nessa rota. Headers inválidos 400; UUID inválido 422; Ticket ausente 404; erro inesperado 500.
- [x] Não foi introduzido evento, outbox, mensageria, idempotency key ou migration.

# 5. Checklist consolidado por TDD

- [x] Controller valida ator/path e executa transação com repositório scoped ao EntityManager; middleware específico recusa bytes de body antes do parser JSON sem mudar outras rotas.
- [x] Caso de uso bloqueia Ticket `FOR UPDATE`, revalida ownership/status sob lock e não consulta Department. A capacidade nova do repository atualiza apenas requesterStatus/updatedAt; RF06 conserva updateTicket restrito a seus próprios campos.
- [x] Update e único AuditLog usam a mesma transação/timestamp; falha induzida do insert reverte ambos. RF08×RF08 produz uma transição/uma auditoria; RF06×RF08 preserva ambas alterações e auditorias independentes.
- [x] OpenAPI documenta RF08 com três headers e ticketId, sem requestBody, 200/400/403/404/422/500, reutilizando Ticket completo.

# 6. Checklist consolidado por TP

| RF          | Unit                                  | Integration                   | Functional/contract                                             | Estado                |
| ----------- | ------------------------------------- | ----------------------------- | --------------------------------------------------------------- | --------------------- |
| RF01–RF06   | Suítes históricas                     | Suítes históricas             | Provas PostgreSQL RF01–RF06 repetidas                           | Passou nesta execução |
| RF07a/RF07b | Suíte histórica                       | Suíte histórica               | Prova PostgreSQL RF07 repetida                                  | Passou nesta execução |
| RF08        | `resolve-ticket.use-case.spec.ts` (6) | `resolve-ticket.spec.ts` (12) | OpenAPI (2 compartilhados), `proof:rf08:postgres`, smoke imagem | Passou localmente     |
| RF09–RF13   | Planejado                             | Planejado                     | 404 e ausência OpenAPI verificados                              | Não implementado      |

Os testes SQLite/HTTP não substituem a prova PostgreSQL de lock e rollback. Nenhum UAT ou CI remoto RF08 foi observado.

# 7. Inventário de endpoints reais

Além das rotas RF01–RF07b, existe apenas `POST /api/support/tickets/:ticketId/resolve` para RF08. `api.http` inclui chamadas válidas, no-op e erros. RF09–RF13 seguem sem handlers. A prova RF08 verificou HTTP 404 para GET Ticket, POST/PATCH/GET mensagens e GET history, além da ausência das operações no OpenAPI.

# 8. Fronteira NFR e capacidades transversais

| Item                                              | Categoria                    | Evidência / limite                                              |
| ------------------------------------------------- | ---------------------------- | --------------------------------------------------------------- |
| Correlação, erros HTTP, PostgreSQL e OpenAPI RF08 | Implementado localmente      | Testes HTTP, contrato e processo compilado                      |
| AuthN e RBAC amplo                                | Upstream/plataforma          | BFF entrega identidade; RF08 aplica ownership fino              |
| Observabilidade distribuída                       | Compartilhado                | Nenhuma obrigação local nova RF08 especificada                  |
| Eventos, outbox, DLQ/redrive e Schema Registry    | Fora do escopo desta release | Sem evento Support aprovado                                     |
| Seed W1                                           | Gap local prévio             | Bloqueada até massa determinística aprovada; não deriva de RF08 |

# 9. Cobertura de testes e validação

`npm run lint`, `build`, `build:check`, `openapi:export`, `openapi:check`, `openapi:compat`, `messaging:check`, `test:unit` (15 suítes/52 testes), `test:integration` (9 suítes/165 testes), `test:contract` (1 suíte/2 testes), `test:coverage` (25 suítes/219 testes), `coverage:check`, `format:check` e `git diff --check` passaram. Cobertura final: 97,96% statements, 86,95% branches, 98,55% functions, 98,32% lines.

As provas PostgreSQL RF01–RF07 passaram novamente em bancos exclusivos `support_s1_proof_rfNN_replay_20260924`, cada um criado e descartado pelo script. RF08 passou no banco `support_s1_proof_rf08_last_20260924`, também descartado. Na execução inicial deste lote, o smoke `proof:s1:image` passou na segunda tentativa; a primeira falha foi investigada na remediação abaixo.

### Remediação do smoke de imagem RF08 — 2026-09-24

O smoke voltou a falhar sem alterações, na checagem final de prontidão do PostgreSQL. Uma cópia temporária instrumentada mostrou `pg_isready` via socket com status `2` (sem resposta), depois `0` (aceitando conexões no servidor temporário do `initdb`), seguida de status `1` (rejeitando conexões durante seu shutdown). Os logs mostraram a criação posterior do banco e a inicialização do servidor definitivo. A causa era o aceite prematuro do servidor temporário, agravado pela segunda sondagem fora do polling; não era falta comprovada de tempo para iniciar o banco.

O harness agora consulta `SELECT 1` no banco descartável pela interface TCP dentro do container, esperando o servidor definitivo e o banco criado, com o mesmo limite de 45 segundos. Falha de prontidão continua encerrando o gate com erro e registra tentativas, tempo, state e logs; o fluxo funcional da imagem não foi relaxado. O runtime RF08 permaneceu intacto.

Três execuções consecutivas após a correção passaram: `a7b1bcacd014` (3 tentativas, 1244 ms), `05a6628bb4e5` (3, 1258 ms) e `189d1d61b7ab` (3, 1247 ms). Cada execução validou health e resolução RF08 na imagem real e confirmou remoção dos próprios containers, rede e tag de imagem antes da próxima.

Após a estabilidade, os gates finais completos passaram novamente: lint, build/build:check, OpenAPI export/check/compat contra `main`, messaging, unit (15 suítes/52 testes), integration (9 suítes/165 testes), contract (1 suíte/2 testes), coverage (25 suítes/219 testes), coverage:check, format:check e `git diff --check`. As provas PostgreSQL RF01–RF08 passaram em bancos exclusivos `support_s1_proof_rfNN_final_20260924`, descartados por seus scripts. O smoke final adicional `3f7d127190f8` passou com SQL/TCP pronto em 3 tentativas (1249 ms), fluxo RF08 e cleanup.

# 10. Divergências

- As duas primeiras tentativas da prova RF08 falharam na asserção de timestamp porque o cliente `pg` da prova interpretava coluna PostgreSQL sem fuso no horário local, enquanto o serviço usa UTC. O parser do script de prova foi alinhado ao serviço; os replays seguintes passaram. Nenhum contrato ou schema foi alterado.
- O smoke da imagem falhava porque `pg_isready` via socket aceitava o servidor temporário do `initdb` antes do shutdown e da subida do servidor definitivo. A causa foi reproduzida e corrigida no harness, conforme a seção de remediação acima.
- Não há novo drift técnico comprovado com Support 0.10. A CI remota RF08 ainda não foi executada porque a branch não foi publicada.

# 11. Conclusão

**Veredito local:** `RF08_IMPLEMENTED_AND_PROVEN_LOCAL`, pronta para revisão e posterior PR. O estado de `main` permanece `MAIN_BASELINE_RF07`. A publicação é tratada em etapa própria, sem merge neste report. Não iniciar RF09 antes da integração RF08 e de seu checkpoint próprio.
