# REPORT — RF01 Criar Departamento

**status:** `RF01_IMPLEMENTED_AND_PROVEN`
**generated_by:** Codex + revisão humana pendente
**generated_at:** 2026-09-10T17:47:42Z
**review_mode:** wave-1 / RF01
**microservice:** support-ms
**repository_ref:** main / `3c387abe2d8f8c99bfa67273210de38b93ea6b10` (worktree local com alterações preservadas)
**documentation_ref:** `luciluci-docs` `76f77340897dc0916476410aed01a622ed796412`; `support/prd.md`, `tdd.md`, `tp.md`, versão 0.3
**report_file:** `docs/reports/REPORT-SUPPORT-RF01-20260910-174742.md`
**reviewer:** não informado

---

# 1. Resumo executivo

- RFs documentadas encontradas: 14 operações (RF01–RF13, com RF07a/RF07b).
- RFs classificadas como **Implementado**: 1 (RF01).
- RFs classificadas como **Parcial**: 0.
- RFs classificadas como **Não encontrado**: 13 operações (RF02–RF13).
- RFs classificadas como **Ambíguo**: 0 neste recorte.
- RFs com matriz **unit + integration + functional** completa: 1, com prova de processo compilado PostgreSQL como exceção funcional documentada.
- RFs com **agrupamento/exceção justificados** na matriz: 1.
- Itens classificados como **Superfície operacional local herdada do template**: health, métricas e Swagger.
- NFRs classificados como **Implementado localmente**: migrations, OpenAPI, correlação, erro HTTP e testes do recorte.
- NFRs classificados como **Upstream/plataforma**: rate limit, Schema Registry externo.
- NFRs classificados como **Compartilhado**: tracing distribuído e política de mensageria.
- NFRs classificados como **Fora do escopo desta release**: carga/performance e segurança automatizada ofensiva.
- NFRs classificados como **Gap real local**: 0 para RF01.

RF01 expõe somente `POST /api/support/departments`: valida DTO, exige
correlação, gera UUID v4, grava Department e memberships em transação local e
retorna `201`. Não introduz ACL local, ator, idempotência de endpoint,
AuditLog, outbox ou evento de domínio. RF02–RF13 permanecem sem rota.

Principal risco técnico-documental: a suíte funcional ampla `FU-SUP-01`
depende de RF02–RF05 e ainda não existe; a prova de processo de RF01 é
estritamente delimitada e não evidencia esse fluxo futuro.

# 2. Escopo e fontes analisadas

Paths de código materializados:

- `src/app.ts`
- `src/features/department/\*\*`
- `src/shared/infrastructure/database/entities.ts`
- `src/shared/infrastructure/database/migrations/1768000000000-CreateSupportDepartments.ts`
- `src/shared/openapi/swagger.ts`
- `scripts/prove-rf01-postgres.js`
- `tests/unit/department/create-department.use-case.spec.ts`
- `tests/integration/department/create-department.spec.ts`
- `tests/contract/openapi/openapi.contract.test.ts`

Estado e documentos consultados: `ACTUAL_STATE.md`, `api.http`,
`docs/openapi/v1/support-api.json`, `docs/architecture/overview.md`,
`docs/runbooks/local-development.md`, `luciluci-docs/support/{prd,tdd,tp,notes}.md`.

Execuções locais concluídas: `npm run lint`, `npm run build`, `npm run
build:check`, `npm run test:unit`, `npm run test:integration`, `npm run
test:contract`, `npm run openapi:export`, `npm run openapi:check`, `npm run
messaging:check`, `npm run proof:rf01:postgres`, `npm run proof:s1:image`,
`npm run test:coverage` e `npm run coverage:check`.

Uma chamada direta de `proof:rf01:postgres` sem `S1_PROOF_*` foi recusada pelo
guard do script, como projetado; a execução aprovada recebeu host, porta,
credenciais, database e porta HTTP explícitos de um PostgreSQL descartável
criado para a prova e o database foi removido pelo próprio script.

Evidência remota de CI/CD:

- `workflow_file`: `.github/workflows/ci.yml`
- `workflow_versioned_present`: sim
- `remote_run_evidence`: ausente
- `remote_run_url`, `remote_run_id`, `remote_head_sha`, `remote_status` e `remote_conclusion`: ausentes

A presença do workflow não é tratada como prova de execução remota.

# 3. Matriz principal RF x implementação

| RF    | Título                        | Endpoint                                                        | Status             | Evidência de código/testes                               | Observação                                    |
| ----- | ----------------------------- | --------------------------------------------------------------- | ------------------ | -------------------------------------------------------- | --------------------------------------------- |
| RF01  | Criar Departamento            | POST `/api/support/departments`                                 | **Implementado**   | feature Department, migration, UT/IT/CT/prova PostgreSQL | Sem efeitos extras; duplicate name permitido. |
| RF02  | Editar Departamento           | PATCH `/api/support/departments/:id`                            | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF03  | Listar Departamentos          | GET `/api/support/departments`                                  | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF04  | Excluir Departamento          | DELETE `/api/support/departments/:id`                           | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF05  | Criar Ticket                  | POST `/api/support/tickets`                                     | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF06  | Editar Ticket                 | PATCH `/api/support/tickets/:id`                                | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF07a | Listar tickets do solicitante | GET `/api/support/tickets/requester/:id`                        | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF07b | Listar tickets admin          | GET `/api/support/tickets/admin/:id`                            | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF08  | Finalizar Ticket              | POST `/api/support/tickets/:id/resolve`                         | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF09  | Buscar Ticket                 | GET `/api/support/tickets/:id`                                  | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF10  | Criar Mensagem                | POST `/api/support/tickets/:id/messages`                        | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF11  | Editar visibilidade           | PATCH `/api/support/tickets/:id/messages/:messageId/visibility` | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF12  | Listar Mensagens              | GET `/api/support/tickets/:id/messages`                         | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |
| RF13  | Listar Histórico              | GET `/api/support/tickets/history`                              | **Não encontrado** | prova retorna 404                                        | Fora do lote.                                 |

# 4. Checklist consolidado por PRD

- [x] RF01: criação validada, UUID v4, `active=true`, array opcional/ordenado e persistência atômica.
- [x] RF01: ausência de ator/chave de idempotência aceita; ausência de correlação rejeitada.
- [x] RF01: sem unicidade de nome, ACL, auditoria, outbox ou evento.
- [ ] RF02–RF13: não implementadas por escopo deliberado.

# 5. Checklist consolidado por TDD

- [x] rota, controller, DTO, use case e repositório TypeORM reais.
- [x] migration PostgreSQL para `departments` e `department_allowed_users` com PK/FK/índice.
- [x] OpenAPI, `api.http`, correlação e mapeamento 400/422/500.
- [x] transação local e rollback comprovado por trigger injetado em PostgreSQL.
- [x] idempotência genérica preservada, porém não ativada na RF01.
- [ ] Ticket, TicketMessage, AuditLog e mensageria de negócio — fora do lote.

# 6. Checklist consolidado por TP

- [x] unitário: `tests/unit/department/create-department.use-case.spec.ts`.
- [x] integração HTTP: `tests/integration/department/create-department.spec.ts`.
- [x] contrato: `tests/contract/openapi/openapi.contract.test.ts`.
- [x] prova funcional delimitada: processo compilado e PostgreSQL descartável via `proof:rf01:postgres`.
- [ ] fluxo E2E `FU-SUP-01`: depende das RF02–RF05, não antecipado.
- [x] matriz explícita RF -> unit / integration / functional abaixo.

## 6.1 Matriz obrigatória RF -> testes

| RF    | Unit                                                       | Integration                                                                                   | Functional                                                       | Agrupamento / exceção                                                      | Status             |
| ----- | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | -------------------------------------------------------------------------- | ------------------ |
| RF01  | `tests/unit/department/create-department.use-case.spec.ts` | `tests/integration/department/create-department.spec.ts`; PostgreSQL em `proof:rf01:postgres` | `scripts/prove-rf01-postgres.js`: processo compilado + HTTP + DB | `FU-SUP-01` depende de RF02–RF05; esta prova isolada é a exceção objetiva. | completa para RF01 |
| RF02  | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF03  | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF04  | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF05  | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF06  | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF07a | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF07b | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF08  | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF09  | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF10  | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF11  | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF12  | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |
| RF13  | ausente                                                    | ausente                                                                                       | ausente                                                          | rota 404 comprovada; não implementada                                      | ausente            |

# 7. Inventário de endpoints reais

## Endpoints funcionais

| Método | Path                       | Handler                       | RF   | Status           |
| ------ | -------------------------- | ----------------------------- | ---- | ---------------- |
| POST   | `/api/support/departments` | `DepartmentController#create` | RF01 | **Implementado** |

## Endpoints operacionais / contratos

| Método | Path             | RF associada        | Status                               |
| ------ | ---------------- | ------------------- | ------------------------------------ |
| GET    | `/health`        | não é RF do domínio | superfície operacional local herdada |
| GET    | `/metrics`       | não é RF do domínio | superfície operacional local herdada |
| GET    | `/api-docs`      | não é RF do domínio | superfície operacional local herdada |
| GET    | `/api-docs-json` | não é RF do domínio | superfície operacional local herdada |

# 8. Fronteira NFR e capacidades transversais

## 8.1 Matriz obrigatória de fronteira NFR

| Item                                         | Categoria                         | Estado local                | Gap real local? |
| -------------------------------------------- | --------------------------------- | --------------------------- | --------------- |
| Migrations, HTTP errors, correlação, OpenAPI | implementado localmente           | comprovados no recorte RF01 | não             |
| Rate limit                                   | upstream/plataforma               | não atribuído a Support     | não             |
| OpenTelemetry/tracing distribuído            | compartilhado                     | sem decisão local adicional | não             |
| Schema Registry, DLQ/redrive e CDC           | upstream/plataforma/compartilhado | mensageria não contratada   | não             |
| Segurança ofensiva e carga                   | fora do escopo desta release      | não executadas              | não             |

## 8.2 Capacidades transversais

| Capacidade           | Status                       | Evidência                                                      |
| -------------------- | ---------------------------- | -------------------------------------------------------------- |
| migrations           | Implementado                 | migration RF01 e prova run/revert/run em PostgreSQL            |
| `api.http`           | Implementado                 | exemplo RF01 alinhado ao request ativo                         |
| seed robusto         | Fora do escopo desta release | seed bloqueada até massa W1 definida                           |
| idempotência da RF01 | Não aplicável                | não há middleware na rota; POSTs repetidos criam IDs distintos |
| correlation id       | Implementado                 | middleware, integração e prova de processo                     |
| OpenAPI              | Implementado                 | export/check, contract e `/api-docs-json` runtime              |
| AsyncAPI/eventos     | Não aplicável                | nenhuma mensageria Support foi inventada                       |
| CI/CD                | Parcial                      | workflow atualizado; sem run remoto comprovado                 |

# 9. Cobertura de testes

`npm run test:coverage` executou 11 suites e 48 testes: Statements 97.14%,
Branches 74.19%, Functions 98.03% e Lines 97.14%. `npm run coverage:check`
passou. As taxas são globais e não substituem a matriz por RF.

RF01 tem prova unitária, integração, contrato e processo compilado PostgreSQL.
Os hotspots futuros são tickets, mensagens, visibilidade, autorização e
auditoria, intencionalmente ausentes neste lote.

# 10. Divergências

## 10.1 Documentação prevê, código não comprova

- RF02–RF13 estão no PRD/TDD/TP, mas não foram iniciadas; `proof:rf01:postgres`
  confirma `404` para suas rotas.

## 10.2 Código existe, documentação não comprova

- Nenhuma divergência RF01 identificada: TDD/TP foram atualizados com tabelas,
  índice, rollback, comandos e caminhos de teste realmente executados.

## 10.3 `ACTUAL_STATE.md` afirma, código não comprova

- Nenhuma no recorte RF01; a afirmação é sustentada por migration, rota, testes
  e prova PostgreSQL.

## 10.4 PRD / TDD / TP divergem entre si

- Nenhuma divergência material para RF01 após o checkpoint 0.3 e atualização de
  evidências. As decisões das demais RFs continuam abertas por desenho.

## 10.5 Ambiguidades que impedem conclusão segura

- O checkpoint RF02 ainda não foi aprovado; edição, listagem e exclusão não
  podem ser inferidas do modelo RF01.
- Não há execução remota de CI comprovada para este HEAD.

# 11. Conclusão

RF01 está aderente ao contrato congelado e comprovada localmente, inclusive em
PostgreSQL descartável e processo compilado. O serviço está em maturidade
parcial: bootstrap e uma RF concluídos, sem antecipar Tickets, mensagens,
auditoria ou mensageria.

Próximo passo: obter o checkpoint documental de RF02 e implementar somente o
novo slice autorizado. Não há commit, push, deploy ou aprovação de produção
associados a este report.
