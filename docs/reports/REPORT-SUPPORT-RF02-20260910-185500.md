# REPORT — RF02 Editar Departamento

**status:** `RF02_IMPLEMENTED_AND_PROVEN`
**generated_by:** Codex
**generated_at:** `2026-09-10T18:55:00Z`
**review_mode:** wave-2 / RF02
**microservice:** `support-ms`
**repository_ref:** `feat/support-rf02-update-department` / `2fe76869158f3eb99ef4dc45453cfa9aedd096eb` (worktree com alterações locais)
**documentation_ref:** `luciluci-docs` `7cc153fab92b634c898727b983078c4c696d3ea9`, branch documental publicada
**report_file:** `docs/reports/REPORT-SUPPORT-RF02-20260910-185500.md`
**reviewer:** não informado

## 1. Resumo executivo

RF02 foi implementada exclusivamente na branch própria, preservando RF01 e
mantendo RF03–RF13 sem rotas. O endpoint `PATCH
/api/support/departments/{departmentId}` valida UUID v4 e PATCH não vazio,
preserva campos omitidos, substitui memberships com ordem/duplicatas, permite
edição de inativo sem restore, usa lock pessimista no Department e executa a
operação no mesmo `EntityManager` transacional.

Não foram adicionados AuditLog, outbox, eventos, idempotência, lookup remoto ou
migration. Não há drift técnico novo conhecido.

## 2. Matriz RF x implementação

| RF                      | Endpoint                                        | Status                   | Evidência                                                        |
| ----------------------- | ----------------------------------------------- | ------------------------ | ---------------------------------------------------------------- |
| RF01                    | `POST /api/support/departments`                 | `IMPLEMENTED_AND_PROVEN` | regressão preservada; testes e prova RF01 anteriores             |
| RF02                    | `PATCH /api/support/departments/{departmentId}` | `IMPLEMENTED_AND_PROVEN` | use case/repository/controller, UT/IT/CT e `proof:rf02:postgres` |
| RF03–RF13 (RF07a/RF07b) | —                                               | `NOT_IMPLEMENTED`        | permanecem sem router; `404` comprovado na prova PostgreSQL      |

## 3. Arquivos alterados

Implementação e contratos locais:

- `src/features/department/**` (DTOs, controller, route, repository, use case e mapper de resposta)
- `src/shared/openapi/swagger.ts`
- `docs/openapi/v1/support-api.json`
- `api.http`
- `package.json`
- `.github/workflows/ci.yml`
- `scripts/prove-rf02-postgres.js`

Testes:

- `tests/unit/department/update-department.use-case.spec.ts`
- `tests/integration/department/update-department.spec.ts`
- `tests/contract/openapi/openapi.contract.test.ts`
- `tests/unit/department/create-department.use-case.spec.ts` (fake atualizado para a interface expandida)

Documentação:

- `ACTUAL_STATE.md`, `AGENTS.md`, `AI_FIRST.md`, `README.md`, `DRIFT_REPORT.md`
- índices, arquitetura, OpenAPI e runbook em `docs/`
- `luciluci-docs/support/{README.md,prd.md,notes.md,tdd.md,tp.md}`

## 4. Validação executada

Passaram:

- `npm run lint`
- `npm run build`
- `npm run build:check`
- `npm run test:unit` — 29 testes
- `npm run test:integration` — 42 testes
- `npm run test:contract` — 2 testes
- `npm run openapi:export`
- `npm run openapi:check`
- `npm run format:check`
- `npm run proof:rf01:postgres` (regressão RF01 em PostgreSQL descartável)
- `npm run proof:s1:image` (smoke da imagem/CMD de produção)
- `npm run proof:rf02:postgres` com PostgreSQL descartável `support_s1_proof_*` e porta HTTP exclusiva

A prova PostgreSQL executou migrations idempotentes, round-trip de todos os
campos, replace/`[]`/duplicatas, no-op e timestamps, edição de inativo,
rollback por falha de membership, lock concorrente, Swagger, processo
compilado e `404` para RF03–RF13; o banco foi removido pelo próprio script.

`npm run test:coverage` executou 73 testes (Statements 97.55%, Branches 77.00%,
Functions 97.22%, Lines 97.50%) e `npm run coverage:check` passou. O smoke da
imagem confirmou o CMD compilado em rede/containers exclusivos.

## 5. Contrato e efeitos

OpenAPI e `api.http` documentam apenas RF01/RF02 e superfícies operacionais.
`Department.active` aceita `false` no schema de resposta, sem alterar o fato de
RF01 sempre criar `active=true`. RF02 não exige headers de ator ou idempotência,
não aplica ACL local e não retorna `403`/`409`.

## 6. Drift, CI e riscos

`DRIFT-SUP-S1-001` permanece `RESOLVED / PROVEN`; nenhum drift novo foi
reproduzido. O workflow versionado inclui a prova RF02, mas não há execução
remota consultada para este HEAD.

O submódulo `luciluci-docs` está na branch `docs/support-rf02-contract`, commit
`7cc153fab92b634c898727b983078c4c696d3ea9`, já publicado no remoto. O serviço
deve registrar esse SHA no gitlink antes do commit da feature.

## 7. PR pronta

Pronta para revisão local na branch `feat/support-rf02-update-department`.
Nenhum commit, push, merge, PR ou deploy foi realizado. RF03 não foi iniciada.
