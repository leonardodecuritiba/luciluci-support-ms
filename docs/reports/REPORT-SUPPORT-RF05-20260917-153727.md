# REPORT - Avaliação de completude do microserviço

**status:** `RF05_IMPLEMENTED_AND_PROVEN`
**generated_by:** Codex
**generated_at:** `2026-09-17T18:37:27Z`
**review_mode:** final / RF05 implementation
**microservice:** `support-ms`
**repository_ref:** `feat/support-rf05-create-ticket`, worktree não commitada
**documentation_ref:** `luciluci-docs` / `cc9a4399d210114e3c8261f3c153f8339c049ffb`
**report_file:** `docs/reports/REPORT-SUPPORT-RF05-20260917-153727.md`
**reviewer:** não informado

# 1. Resumo executivo

- RFs documentadas encontradas: 14 operações (RF01–RF13, com RF07a/RF07b).
- RFs classificadas como **Implementado**: 5 (RF01–RF05).
- RFs classificadas como **Parcial**: 0.
- RFs classificadas como **Não encontrado**: 9 (RF06–RF13, incluindo RF07a/RF07b).
- RFs classificadas como **Ambíguo**: 0.
- RFs com matriz unit + integration + functional/prova completa: 5.
- NFRs classificados como gap real local: 0.

RF05 foi implementada exclusivamente como `POST /api/support/tickets`. O fluxo
abre uma transação, bloqueia Department, valida existência/atividade e cria
Ticket, uma mensagem inicial, mídias posicionais e exatamente um AuditLog
`criacao_ticket`. O número é alocado por sequence PostgreSQL, global, positivo,
único e gapful. Não há evento, outbox, broker, idempotency key ou rota RF06+.

Todos os gates locais, as provas PostgreSQL RF01–RF05 e o smoke da imagem
passaram. A branch ainda não foi commitada, publicada ou submetida a CI remoto/PR.

# 2. Escopo e fontes analisadas

Fontes canônicas: `luciluci-docs/support/{README,prd,notes,tdd,tp,dependencies}.md`,
fonte original preservada e contrato 0.7 no SHA
`cc9a4399d210114e3c8261f3c153f8339c049ffb`.

Código/contratos analisados e alterados:

- `src/features/ticket/**`, `src/app.ts` e registro de entities;
- migration `1768010000000-CreateSupportTickets.ts`;
- `src/shared/openapi/swagger.ts`, OpenAPI exportado e `api.http`;
- `tests/unit/ticket/**`, `tests/integration/ticket/**` e contrato OpenAPI;
- `scripts/prove-rf01-postgres.js` a `prove-rf05-postgres.js`, smoke da imagem e CI.

Execuções objetivas: lint, build, build check, OpenAPI export/check/compat,
mensageria inativa, suites unit/integration/contract, cobertura, proofs
PostgreSQL RF01–RF05, smoke de imagem, format e diff check. CI remoto da feature:
ausente, pois não houve commit/push/PR nesta execução.

Falhas intermediárias registradas: a primeira suíte encontrou um `delete` não
aceito pelo TypeScript no teste novo e a expectativa histórica RF05=404 da
integração RF04; ambos foram corrigidos. A primeira proof RF05 assumia
incorretamente que ordem de conclusão concorrente equivalia à ordem da sequence;
a asserção passou a validar unicidade e sequência crescente após ordenação. A
primeira rerun RF01 ainda esperava o conjunto OpenAPI anterior e foi atualizada
para incluir somente a nova rota RF05. A chamada direta de Prettier sobre
`api.http` não encontrou parser; o gate real `format:check` passou. Nenhuma
dessas falhas finais correspondeu a drift de runtime ou contrato.

# 3. Matriz principal RF x implementação

| RF    | Título                          | Endpoint                                         | Status         | Evidência principal                           |
| ----- | ------------------------------- | ------------------------------------------------ | -------------- | --------------------------------------------- |
| RF01  | Criar Departamento              | `POST /api/support/departments`                  | Implementado   | testes + proof RF01                           |
| RF02  | Editar Departamento             | `PATCH /api/support/departments/{departmentId}`  | Implementado   | testes + proof RF02                           |
| RF03  | Listar Departamentos            | `GET /api/support/departments`                   | Implementado   | testes + proof RF03                           |
| RF04  | Excluir Departamento            | `DELETE /api/support/departments/{departmentId}` | Implementado   | testes + proof RF04                           |
| RF05  | Criar Ticket                    | `POST /api/support/tickets`                      | Implementado   | feature Ticket + testes + proof RF05 + imagem |
| RF06  | Editar Ticket                   | contrato canônico                                | Não encontrado | `404` comprovado                              |
| RF07a | Listar Tickets do Solicitante   | contrato canônico                                | Não encontrado | `404` comprovado                              |
| RF07b | Listar Tickets (Admin)          | contrato canônico                                | Não encontrado | `404` comprovado                              |
| RF08  | Finalizar Ticket (solicitante)  | contrato canônico                                | Não encontrado | `404` comprovado                              |
| RF09  | Buscar Ticket por ID            | contrato canônico                                | Não encontrado | `404` comprovado                              |
| RF10  | Criar Mensagem do Ticket        | contrato canônico                                | Não encontrado | `404` comprovado                              |
| RF11  | Editar Visibilidade da Mensagem | contrato canônico                                | Não encontrado | `404` comprovado                              |
| RF12  | Listar Mensagens do Ticket      | contrato canônico                                | Não encontrado | `404` comprovado                              |
| RF13  | Listar Histórico (genérico)     | contrato canônico                                | Não encontrado | `404` comprovado                              |

# 4. Checklist consolidado por PRD

- [x] RF05 cria Ticket com UUID v4, statuses iniciais e timestamps iguais.
- [x] `number` vem de sequence PostgreSQL única e aceita gaps de rollback.
- [x] Mensagem inicial deriva author/type/visibilidade e não gera `nova_mensagem`.
- [x] `mediaIds` omitido vira zero rows; ordem e duplicatas são preservadas.
- [x] Exatamente um AuditLog `criacao_ticket`, com status fields nulos.
- [x] Department inexistente retorna 404; inativo retorna 422 `department_inactive`.
- [x] Response 201 contém somente Ticket e não exige `Location`.
- [ ] RF06–RF13 permanecem não implementadas.

# 5. Checklist consolidado por TDD

- [x] Controller abre uma única transação e usa repositories scoped ao manager.
- [x] Department é lido com lock pessimista antes de qualquer insert RF05.
- [x] Inserts explícitos tornam ordem e rollback observáveis.
- [x] Tabelas `tickets`, `ticket_messages`, `ticket_message_media` e
      `ticket_audit_logs` possuem FKs/constraints locais.
- [x] Migration executa, reexecuta sem pendência, reverte só RF05 e reaplica.
- [x] DTO usa whitelist/reject unknown e enums literais.
- [x] Não há Files lookup/FK, RBAC fino, mensageria, evento ou outbox.

# 6. Checklist consolidado por TP

- [x] Unit cobre mapping, defaults, UUIDs, ordem dos writes e propagação de erro.
- [x] Integração HTTP cobre sucesso, persistência, validação e rotas futuras.
- [x] Contrato cobre body/enums/media opcional/response/erros/ausência RF06+.
- [x] PostgreSQL real cobre schema, rollback, sequence, paralelismo e RF04×RF05.
- [x] Smoke da imagem cobre CMD real, migration, Department e Ticket RF05.

## 6.1 Matriz obrigatória RF -> testes

| RF    | Unit                                 | Integration                 | Functional                     | Status               |
| ----- | ------------------------------------ | --------------------------- | ------------------------------ | -------------------- |
| RF01  | `create-department.use-case.spec.ts` | `create-department.spec.ts` | `proof:rf01:postgres`          | completa com exceção |
| RF02  | `update-department.use-case.spec.ts` | `update-department.spec.ts` | `proof:rf02:postgres`          | completa             |
| RF03  | `list-departments.use-case.spec.ts`  | `list-departments.spec.ts`  | `proof:rf03:postgres`          | completa             |
| RF04  | `delete-department.use-case.spec.ts` | `delete-department.spec.ts` | `proof:rf04:postgres`          | completa             |
| RF05  | `create-ticket.use-case.spec.ts`     | `create-ticket.spec.ts`     | `proof:rf05:postgres` + imagem | completa             |
| RF06  | ausente                              | ausente                     | ausente                        | ausente              |
| RF07a | ausente                              | ausente                     | ausente                        | ausente              |
| RF07b | ausente                              | ausente                     | ausente                        | ausente              |
| RF08  | ausente                              | ausente                     | ausente                        | ausente              |
| RF09  | ausente                              | ausente                     | ausente                        | ausente              |
| RF10  | ausente                              | ausente                     | ausente                        | ausente              |
| RF11  | ausente                              | ausente                     | ausente                        | ausente              |
| RF12  | ausente                              | ausente                     | ausente                        | ausente              |
| RF13  | ausente                              | ausente                     | ausente                        | ausente              |

# 7. Inventário de endpoints reais

| Método | Path                                      | Handler                       | RF   | Status       |
| ------ | ----------------------------------------- | ----------------------------- | ---- | ------------ |
| POST   | `/api/support/departments`                | `DepartmentController.create` | RF01 | Implementado |
| PATCH  | `/api/support/departments/{departmentId}` | `DepartmentController.update` | RF02 | Implementado |
| GET    | `/api/support/departments`                | `DepartmentController.list`   | RF03 | Implementado |
| DELETE | `/api/support/departments/{departmentId}` | `DepartmentController.delete` | RF04 | Implementado |
| POST   | `/api/support/tickets`                    | `TicketController.create`     | RF05 | Implementado |

`/health`, `/metrics`, `/api-docs` e `/api-docs-json` permanecem superfícies
operacionais herdadas, não RFs do domínio. RF06–RF13 não têm handlers.

# 8. Fronteira NFR e capacidades transversais

| Item                               | Categoria                    | Evidência local                | Gap real local? |
| ---------------------------------- | ---------------------------- | ------------------------------ | --------------- |
| correlação/error mapping           | implementado localmente      | middleware + suites HTTP       | não             |
| PostgreSQL/atomicidade/lock        | implementado localmente      | proof RF05 real                | não             |
| OpenAPI/compatibilidade            | implementado localmente      | export/check/compat verdes     | não             |
| RBAC amplo                         | upstream/plataforma          | BFF/borda                      | não             |
| eventos/broker                     | fora do escopo desta release | nenhum evento Support aprovado | não             |
| OpenTelemetry/DLQ/redrive          | compartilhado/fora do escopo | sem decisão local RF05         | não             |
| segurança/performance automatizada | fora do escopo desta release | não exigida pelo slice         | não             |

O workflow preserva todos os gates anteriores e adiciona RF05 com banco/porta
exclusivos. A tabela genérica de idempotência permanece vazia e não participa
do endpoint. A sequence substitui qualquer contador em memória/lock global.

# 9. Cobertura de testes

Método: `npm run test:coverage`, seguido de `npm run coverage:check`.

- Suites: 19 passed / 19 total.
- Testes: 146 passed / 146 total.
- Statements: 98,18% (596/607).
- Branches: 83,68% (118/141).
- Functions: 98,09% (103/105).
- Lines: 98,13% (578/589).

As cinco RFs implementadas têm evidência focal e prova funcional. RF06–RF13
não possuem cobertura de implementação porque permanecem fora do runtime.

# 10. Divergências

## 10.1 Documentação prevê, código não comprova

RF06–RF13 seguem previstas e deliberadamente não implementadas.

## 10.2 Código existe, documentação não comprova

Nenhuma divergência encontrada. A adaptação SQLite aloca `number` apenas para
testes in-process; produção foi provada com a sequence PostgreSQL canônica.

## 10.3 `ACTUAL_STATE.md` afirma, código não comprova

Nenhuma após esta atualização.

## 10.4 PRD / TDD / TP divergem entre si

Nenhuma divergência RF05 encontrada contra a revisão 0.7. `luciluci-docs` não
foi alterado.

## 10.5 Ambiguidades que impedem conclusão segura

Nenhuma para RF05. Contratos posteriores continuam dependentes de checkpoints
próprios.

# 11. Conclusão

Aderência funcional e técnico-operacional RF05: completa no recorte congelado.
Estado final: `RF05_IMPLEMENTED_AND_PROVEN` na branch funcional.

Não há drift técnico aberto. A branch está pronta para commit, push, CI remoto e
PR, mas essas ações não foram autorizadas neste lote. RF06 não deve começar
antes da integração da RF05 e de checkpoint próprio.
